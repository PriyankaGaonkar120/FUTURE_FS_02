import CheckoutForm from "../components/CheckoutForm";

const Checkout = () => {
  return (
    <div style={{ padding: "2rem" }}>
      <h2>Checkout</h2>
      <CheckoutForm />
    </div>
  );
};

export default Checkout;
